<h4>Login Info</h4>
<table id='adminloginstats' class='table table-striped table-bordered table-condensed'>
  <thead>
    <tr>
      <th>Sign In Count</th>
      <th>Current Sign In </th>
      <th>Last Sign In</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @user.sign_in_count.to_s(:long) unless @user.sign_in_count.blank? %></td>
      <td><%= @user.current_sign_in_at.to_s(:long) unless @user.current_sign_in_at.blank?  %></td>
      <td><%= @user.last_sign_in_at.to_s(:long) unless @user.last_sign_in_at.blank?  %></td>
    </tr> 
  </tbody>  
</table>